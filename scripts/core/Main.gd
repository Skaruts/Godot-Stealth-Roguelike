extends Node

var frames = []

func _ready() -> void:
	pass











#   BOX CONTAINER TESTS
#
#	var box = ui.new_hbox_container(self, 0, 0, Data.GW, Data.GH)
##	var box = ui.new_vbox_container(self, 0, 0, Data.GW, Data.GH)
#
#	frames.append( ui.new_frame(box, 1,1,1,1) )
#	frames.append( ui.new_frame(box, 1,1,1,1) )
#	frames.append( ui.new_frame(box, 1,1,1,1) )
##	frames.append( ui.new_frame(hbox, 1,1,1,1) )
##	frames.append( ui.new_frame(hbox, 1,1,1,1) )
##	frames.append( ui.new_frame(hbox, 1,1,1,1) )
##	frames.append( ui.new_frame(hbox, 1,1,1,1) )
#
#	frames[0].set_min_size(20, 18)
##	frames[3].set_min_size(20, 9)
#
#	for f in frames:
#		f.expand_h = true
#		f.expand_v = true
#
#	for f in frames:
#		box.add_child(f)
#
#	frames[0].expand_v =false
#	frames[2].expand_v =false
##	frames[1].expand_h =false
##	frames[2].expand_h =false
##	frames[2].set_visible(false)
##	frames[0].set_visible(false)
#
#	pass